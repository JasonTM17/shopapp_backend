## Summary

- What changed:
- Why:

## Validation

- [ ] `./mvnw -q -DskipTests compile` passed
- [ ] Health endpoint verified (`/api/v1/actuator/health`)
- [ ] No runtime artifacts committed (`logs`, `dump.rdb`, `.DS_Store`)

## Notes

- Migration/config impact:
- Follow-up tasks:
